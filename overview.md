# 交叉验证交付总览 — moeplay-tauri

**全栈清理 / 前后端修复 / 核心模块全功能测试 · 多子代理并行 + 交叉验证**

## 闸门结果（全绿）
- `cargo check` ✅ 0 错误（tauri-build 已通过能力解析 + 权限生成）
- `svelte-check` ✅ 0 错误 / 0 警告
- `vitest` ✅ 107/107 通过
- 悬空引用扫描 ✅ 源码级 0 处（仅保留有意功能）
- capability↔command 一致性 ✅

## 子代理分工（4 泳道，文件集合互斥）
- **A 清理**：`.playwright-mcp/` / `.gitignore`
- **B 游戏库 QA**：`gameLibrary.svelte.ts` + 契约核对（修取消误报 Bug）
- **2 播放器**：`video_extractor.rs` / `video_proxy.rs`（根因A 广告劫持 + 根因B HLS 嗅探）
- **3 迁移移除**：删除 C# 导入全套
- **D 闸门（主代理）**：全量编译/类型/引用/能力扫描

## 交叉验证机制
编译闸门(cargo check) → 类型闸门(svelte-check) → 悬空引用扫描 → capability 一致性(tauri-build) → 契约对等(前端 invoke↔Rust command) → 第二人复检(播放器 Agent C 复检 Agent 2) → 单元+手动测试矩阵。

## 关卡价值：D 拦截了 S8 的 4 处悬空引用
1. `build.rs` 残留 `migrate_from_csharp` → command not found
2. `sensitive.json` 残留 `allow-migrate-from-csharp` → 能力解析失败
3. `examples/moe-migrate.rs` 仍 import 已删模块
4. `permissions/autogenerated/` 3 个孤儿权限 toml（非 gitignore，已跟踪）

## 详细交付物
见 `plans/cross-validation-report.md`（任务拆解 / 分工策略 / 验证机制 / 双模块测试矩阵）。
