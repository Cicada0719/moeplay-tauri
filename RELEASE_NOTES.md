# 萌游 MoeGame v0.20.0

## 番剧源更新与搜索可靠性

- 番剧规则源支持自动检查更新：启动时静默比对 Kazumi 规则仓库版本，"规则"页显示可更新数量角标，新增"全部更新"一键只更新已安装规则（区别于"全部安装"）。
- 规则目录本地缓存：GitHub 拉取失败时回退展示上次缓存的目录，不再一片空白。

## 番剧搜索失败可见

- 搜索不再静默吞掉失败源：结果页顶部显示"X 源成功 · Y 源无结果 · Z 源失败"摘要，失败源可展开查看原因（超时 / HTTP 错误等），并可"重试失败源"单独补搜。
- 搜索接入源健康度调度：连续失败的源自动排到后面，健康的源优先出结果；搜索成败计入源健康档案。

## 播放页沉浸模式

- 番剧播放 20 秒后自动隐藏顶部任务栏，画面更大；暂停立即恢复，指针移到窗口顶部即可唤回，离开约 3 秒后再收起。仅播放页生效，退出播放页自动恢复。

## 发布与兼容性

- 版本号已统一更新至 0.20.0。
- 本版本面向 Windows x64，提供 **NSIS 安装版**（推荐，支持应用内签名自动更新）、**MSI 安装版**（适合集中部署）和 **便携包**，所有安装包均已签名。

**不包含 APK 或 AAB。**
