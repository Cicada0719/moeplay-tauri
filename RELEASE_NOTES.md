# 萌游 MoeGame v0.23.0

本节记录待发布变更；实际可下载状态以 GitHub Release 和下载站清单为准。

- 漫画单页位置续读，单双页切换与跨页大图保留页码；普通图片源和 Provider v2 接入本地阅读历史。
- 小说历史按书展示，各章进度独立保留，恢复最近访问位置；连续读超过 60 章不再挤掉其他书。
- 旧 localStorage 记录首次幂等迁移，原件保留；统一历史支持搜索、筛选、删除与版本化 JSON 备份，移动端提供文本复制恢复。
- “无界流光”改为墨黑、暖白与电光青，新安装默认启用；已有主题选择和壁纸保留。
- Windows 官方构建使用 HTTPS 签名更新及 GitHub 备用入口；普通 Fork 构建不需要官方密钥。
- Android 正式包使用长期证书，兼容包沿用旧证书；覆盖升级验证状态见清单。不同证书无法直接覆盖安装。
- 下载站采用本地素材、真实应用截图（演示资料）和统一哈希清单，支持版本目录与回滚；阅读历史 v2 本轮不接入 WebDAV。

---

# 历史：萌游 MoeGame v0.22.1

## 局域网自动更新上线

- 更新器端点切换至内网更新服务器（`http://192.168.2.88:8788/latest.json`），安装包放入服务器即全内网客户端自动收到更新。
- 新增 `update-server/` 一键部署包：PowerShell 自托管服务 + 下载页（最新版本/历史版本）+ 发布令牌鉴权，零外部依赖。
- 新增一条命令发布：`npm run release:win`（构建→minisign 签名→原子发布 latest.json）与 `npm run publish:update`。
- 更新签名密钥轮换为新 minisign 密钥对（v0.22.0 及更早客户端需手动安装一次 v0.22.1 作为桥接版本，此后全部自动更新）。
- 合并掌机终版线与桌面 0.22.0 主页增强线；恢复 updater/process 能力权限。

# 萌游 MoeGame v0.22.0

## 主页信息整合与展示增强

- 「今日放送」：番剧主页与今日继续页展示 Bangumi 当日放送，可直达完整时间表。
- 游戏主页：恢复「继续游玩」列表 + 库概览统计（总数/已安装/游玩中/累计时长）+ 任务状态胶囊。
- 漫画主页：「继续阅读」大卡片 rail + 漫画卡片阅读进度徽标 + 收藏/历史空态引导。
- 漫画阅读器：单页/双页跨页排版 + 阅读进度条，方向与单双页偏好记忆。
- 命令面板：Ctrl+K 全局搜索游戏 / 导航 / 命令（拼音匹配）；聚焦搜索改 Ctrl/Shift+K。
- 迷你置顶播放窗：番剧一键小窗置顶播放（无边框、可拖拽、HLS/直链）。

## 安装

- Windows x64：安装版（NSIS，推荐）/ MSI / 便携包，均在 `src-tauri/target/release/bundle/` 下。

---

# 历史：萌游 MoeGame v0.21.0

## 掌机漫画阅读适配

- 漫画阅读器为掌机模式做了适配：工具栏与按键放大到至少 44px，抽幅翻页热区加宽，单页高度适配小屏，掌机上读漫画更顺手。

## 安装

- Windows x64：安装版（NSIS，推荐）/ MSI / 便携包，均在 `src-tauri/target/release/bundle/` 下。

---

# 历史：萌游 MoeGame v0.20.37

## 全量验收

- 30 轮持续优化的收官版本：全部门禁复核通过，功能与稳定性保持。

---

# 历史：萌游 MoeGame v0.20.36

## 大屏转盘三态视图

- 大屏游戏列表可用 View 键在 全部 / 本机已装 / 最近游玩 间切换，方便电视场景快速回到在玩的游戏。

---

# 历史：萌游 MoeGame v0.20.35

## 内部重构（无行为变化）

- 番剧观看历史独立成文件并补测试，store 持续瘦身，继续观看联动保持不变。

---

# 历史：萌游 MoeGame v0.20.34

## 内部重构（无行为变化）

- 番剧搜索历史独立成文件并补测试，store 持续瘦身。

---

# 历史：萌游 MoeGame v0.20.33

## 稳定性加固

- 为播放器偏好模块新增单元测试，覆盖默认值、持久化与非法值归一化。

---

# 历史：萌游 MoeGame v0.20.32

## 内部重构（无行为变化）

- 番剧 store 的播放器偏好独立成文件，store 持续瘦身。

---

# 历史：萌游 MoeGame v0.20.31

## 稳定性加固

- 图片搜番与章节评论模块补齐单元测试，拆出的域模块测试覆盖完整。

---

# 历史：萌游 MoeGame v0.20.30

## 稳定性加固

- 为弹幕模块新增单元测试，覆盖弹幕搜索/加载与集数匹配逻辑。

---

# 历史：萌游 MoeGame v0.20.29

## 修复

- 大屏记忆在媒体展厅场景的恢复位置修正。

---

# 历史：萌游 MoeGame v0.20.28

## 稳定性加固

- 为收藏集模块新增单元测试，覆盖增删改、类型过滤与本地持久化。

---

# 历史：萌游 MoeGame v0.20.27

## 内部重构（无行为变化）

- 番剧 store 的收藏集模块独立成文件，store 持续瘦身。

---

# 历史：萌游 MoeGame v0.20.26

## 文档

- README 更新近期版本亮点，覆盖手柄/掌机/大屏/性能优化。

---

# 历史：萌游 MoeGame v0.20.25

## 内部重构（无行为变化）

- 番剧 store 的图片搜番模块独立成文件，store 持续瘦身。

---

# 历史：萌游 MoeGame v0.20.24

## 内部重构（无行为变化）

- 番剧 store 的弹幕模块独立成文件，store 持续瘦身，可维护性提升。

---

# 历史：萌游 MoeGame v0.20.23

## 内部重构（无行为变化）

- 番剧 store 的章节评论模块拆分为独立文件，为后续持续拆分打样。

---

# 历史：萌游 MoeGame v0.20.22

## 稳定性加固

- 为 API 层新增契约测试，全面回归通过，为后续优化保障安全网。

---

# 历史：萌游 MoeGame v0.20.21

## 屏幕键盘布局记忆

- 掌机屏幕键盘记住上次用的字母/符号面板，打开即用。

---

# 历史：萌游 MoeGame v0.20.20

## 大屏媒体展廊

- 大屏「媒体展廊」现在可以直接浏览全部追番与漫画收藏，手柄一键直达想看的内容。

---

# 历史：萌游 MoeGame v0.20.19

## 内部重构（无行为变化）

- API 调用层按领域拆分为 12 个模块，便于后续维护与按需加载；对外接口与包体不变。

---

# 历史：萌游 MoeGame v0.20.18

## 稳定性

- 修复按键绑定在特殊时序下不生效的问题；为手柄运行时新增 4 个集成测试（重映射、布局覆盖、灵敏度死区）。

---

# 历史：萌游 MoeGame v0.20.17

## 串流手感调节

- 设置页新增手柄灵敏度：摇杆死区（松/标准/紧）与连发速度（慢/标准/快）可调，UU远程 等远程串流延迟高时把死区调松、连发放慢，操作更跟手。

---

# 历史：萌游 MoeGame v0.20.16

## PlayStation 手柄与大屏记忆

- DualShock/DualSense 现在显示 ○×□△ 键帽（✕=确认、○=取消），设置页可手动指定 PlayStation 布局。
- 大屏模式会记住上次的展厅、位置与筛选状态。

---

# 历史：萌游 MoeGame v0.20.15

## 手柄按键绑定

- 设置页可以为每个动作（启动、返回、收藏、档案、翻页、筛选、开始）绑定到手柄上的任意按钮；提示条与按键提示会跟随显示当前绑定的键。适合把操作按自己的习惯排列（例如 UU远程 串流时把确认换到顺手的位置）。

---

# 历史：萌游 MoeGame v0.20.14

## 更轻的主程序

- 手柄相关代码改为按需加载：不用手柄的用户不再加载手柄导航模块（主入口脚本 360KB）。

---

# 历史：萌游 MoeGame v0.20.13

## 更轻的主程序

- 任务中心相关界面组件改为按需加载，主入口脚本进一步缩小（380KB）。

---

# 历史：萌游 MoeGame v0.20.12

## 启动再加速

- 番剧/漫画数据模块改为按需加载（主入口脚本 396KB，继续缩小约 15%），首页继续观看功能保持不变。

---

# 历史：萌游 MoeGame v0.20.11

## 更快、更稳

- 英文词典按需加载：默认中文界面不再下载英文文案，主入口脚本进一步缩小（468KB）。
- 修复一个存量失败的截图用例（改为功能性断言），本机字体/渲染差异不再影响测试结果。

---

# 历史：萌游 MoeGame v0.20.10

## 启动更快

- 大屏模式页面与快捷键帮助、更新弹窗全部改为按需加载，主入口脚本再缩小约 10%，启动更轻。

---

# 历史：萌游 MoeGame v0.20.9

## 掌机模式联动

- 设置页「掌机模式」现在统一管理掌机体验：显示放大之外，还可开启「手柄提示条常显」（连上手柄直接看到操作提示）和「自动屏幕键盘」（点任何输入框自动弹出屏幕键盘，掌机没有实体键盘也能打字）。

---

# 历史：萌游 MoeGame v0.20.8

## 手柄：串流虚拟手柄按设备记忆布局（UU远程）

- **UU远程 的远端手柄现在可以正确识别为 Switch / Xbox 了**：UU远程 的虚拟手柄在系统里报的是与真实 Xbox 完全相同的名称，自动识别无法分辨远端实体类型。现在设置页新增「已连接手柄」列表，每个手柄可单独指定 自动 / Xbox / 任天堂 布局并永久记忆（按「设备 id#槽位」保存），同一台机器上混插真实 Xbox 与 UU远程 虚拟手柄也互不影响。
- 手柄提示条现在列出所有已连接手柄的名称和当前布局标签（Xbox / 任天堂），一眼就能确认识别结果；识别不对时到设置页手动纠正即可。
- 任天堂布局下 A（右）= 确认、B（下）= 取消的映射保持不变。

## 大屏模式：键位说明与实际行为一致

- 修复底部提示条标注颠倒的问题：A=启动游戏、Y=打开档案、X=收藏、View=切换筛选、B=菜单。
- START 键在转盘 / 英雄区 / 媒体区 / 顶部导航都绑定为「启动 / 确认」，与提示一致。
- 提示条显示当前手柄布局标签；顶部导航与工具按钮增加悬停反馈。
- 掌机小屏（5.5-8 寸）上媒体区、转盘、英雄区进一步放大，720p 屏不拥挤。

## 包体与启动优化

- 主入口脚本缩小约一半（1.07MB → 548KB），启动更快：统计图表（chart.js）与拼音检索库改为按需/并行加载。
- 默认库背景图从 1.59MB PNG 换成 76KB WebP；字体只打包拉丁子集。
- Rust 侧：tokio 按需特性 + 发布构建优化（thin LTO / 去符号），安装包更小。
- 开发调试日志全部收敛到开发模式，正式版控制台更干净。

---

## 历史：v0.20.7 更新内容

## 手柄：串流/虚拟手柄修复与主页精简

- **UU远程等串流工具的手柄现在可用了**：手柄输入改为「全部已连接手柄合并读取」——串流/映射工具注册的占位虚拟手柄不再抢占输入，真实手柄无论排在哪个槽位都能操作；多个手柄各自套用正确的按键布局（Xbox/任天堂混插也没问题）。
- 手柄提示条现在显示已连接手柄的名称（如 "Xbox 360 Controller"），方便确认系统是否识别到手柄。若用 UU远程的「按键映射」方案，手柄会被映射成键鼠而非手柄——请在 UU远程里切换到「Xbox 手柄方案」。
- 主页精简：移除立方舞台的"折叠"功能与底部按键说明行。

## 掌机适配（5.5-8 寸，720p-1080p）

- **任天堂手柄支持**：自动识别 Switch Pro / Joy-Con（含 vendor 057e），A（右）= 确认、B（下）= 取消，符合任天堂习惯；Xbox 手柄不变。设置页新增「手柄按键布局」（自动 / Xbox / 任天堂）可手动覆盖。
- **掌机小屏适配**：设置页新增「掌机适配」（自动 / 开 / 关）。自动模式命中 720p/800p 矮横屏及触屏掌机 1080p（Steam Deck / ROG Ally 类）；生效后大屏模式的字号、转盘卡片、操作按钮、手柄提示条全面放大，普通模式下手柄可激活目标不小于 40px。
- 沿用 Steam 式双层结构：桌面模式 + 大屏/掌机模式（START 键或 dock「大屏」进入），不做独立安装包。

## 番剧源：内置源库 + 自动同步（0.20.1-0.20.4 管线成果）

- 内置 4 条实测可用的 Kazumi 兼容番剧源（AGE / MXdm / gugu3 / xfdmneo），开箱即看，内置源不可删除、升级自动补回。
- KazumiRules 官方源启动自动同步导入（主源 + 镜像双通道，按版本增量更新），规则页可一键同步。
- 新规则引擎（#7）：JS 规则沙箱执行、源切换状态保持、健康检查与超时保护。
- 规则现在磁盘持久化（`anime_rules.json`），清除浏览器数据不再丢规则；localStorage 为空时自动从后端恢复。

## 刮削源健壮性与结果完整性

- **vndb / bangumi / getchu 全部接入代理与自动重试**（此前忽略代理设置且无重试，是"源经常不可用"的主因）；touchgal/kungal 不再重复请求同一 API；被反爬软拦截的空结果不再缓存 1 小时。
- 刮削弹窗：失败源原因直接可见，支持"重试失败源"（成功源走缓存秒回）。
- 详情补全失败时明确提示"资料不完整"，游戏详情页出现"资料不完整 · 点击刮削补全"标记；修复刮削应用时空 tags 清掉已有标签的问题。

## 其他改进

- 漫画双页阅读（#12）、WebDAV 历史同步（#11）、历史数据模型 v2 自动迁移（#10）、播放器超清控制栏修复与错误降级（#9）。
- 番剧页包体优化：AnimePage chunk 680KB → 75KB，播放器（含 hls.js）改为播放时按需加载，启动更快。
- 番剧搜索：逐源状态摘要、失败重试、规则页健康度展示（成功率 / 连续失败）。

## 发布与兼容性

- 版本号已统一更新至 0.20.7（0.20.1-0.20.5 为流水线内部迭代，未发布）。
- 本版本面向 Windows x64，提供 **NSIS 安装版**（推荐，支持应用内签名自动更新）、**MSI 安装版**（适合集中部署）和 **便携包**，所有安装包均已签名。

**不包含 APK 或 AAB。**
