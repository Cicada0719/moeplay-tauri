# 参与萌游开发

先阅读 [AGENTS.md](AGENTS.md)、[PRD](docs/PRD.md) 和对应 `specs/task-*.md`。PRD 与规格是契约；发现差异请写在 Issue / PR 中，不直接修改 `docs/`、`specs/`。

## 开始

通过 Issue 说明问题、预期行为及本次修改的目录。使用 `feat/issue-<编号>` 分支，保留已有工作区改动，只修改声明范围。跨模块修改先在 Issue 中列出范围。

```sh
npm ci
npm run doctor
npm run tauri -- dev
```

`src/lib/` 包含界面、状态和前端功能；`src-tauri/src/` 是 Rust；`plugins/` 是本地 Tauri 插件；`tests/visual/` 是浏览器回归；`update-server/` 是下载站与部署文件。使用 npm，不额外提交其他包管理器的锁文件。

## 提交前检查

```sh
npm run verify:versions
npm run verify:commands
npm run check
npm run test:unit
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml --all-targets
npm run build
npm run test:visual
```

为新行为添加测试。视觉修改更新基准后，检查截图差异并在 PR 中附图，不能用忽略截图参数跳过比较。新增 Tauri 命令须同步命令注册、build.rs、capabilities 与 autogenerated permissions。

提交格式：`feat: #<编号> <说明>`。PR 标题格式一致；包含关联 Issue、变更与规格差异、测试结果及规格链接。不要提交私钥、keystore、密码、个人服务器配置、日志、测试输出和构建产物。

## 构建与分发

普通 Fork 的默认 Tauri 构建不依赖官方密钥；正式签名与更新端点通过独立配置启用。发行附件必须来自完成验收的同一提交，同批文件发布到 GitHub 与下载站，并核对 SHA-256。Android 更换证书会影响覆盖升级，请明确渠道并验证旧证书。

只读依赖审计不会自动修改锁文件。依赖升级应独立提交、解释原因并跑测试。详细发布配置见 README 与 `release.config.example.json`。
