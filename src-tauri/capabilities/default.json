{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限 — 允许前端调用所有自定义命令",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
